<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:rooms, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:rooms, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :room, url(:rooms, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'rooms/form', :locals => { :f => f } %>
 <% end %>
</div>
